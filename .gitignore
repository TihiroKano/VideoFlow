# 依赖与构建产物（全部位于 D 盘，不写入 C 盘）
node_modules/
dist/
src-tauri/target/
*.local

# 运行时产物
downloads/
*.part
*.resume.json

# sidecar 与打包产物：体积过大（ffmpeg/ffprobe 各约 98 MB），不进仓库。
# 需要时自行放进 bin/，见 README「准备 sidecar」一节。
bin/
*.exe
WebView2Loader.dll

# 调试截图（体积大、只用于本地比对；生成脚本保留）
test/_analysis/
test/**/*.png

# 编辑器
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 日志
*.log
npm-debug.log*

# 工具目录
.trae/